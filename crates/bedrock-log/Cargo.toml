[package]
name = "bedrock-log"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Conditional logging macros for bedrock - uses pr_info!/pr_err! in kernel, println! in tests"
build = "build.rs"

[features]
default = ["cargo"]
cargo = []
