[storage]
# Storage driver - vfs works on any filesystem (including tmpfs/initrd)
driver = "vfs"

# Root directory for storage
graphroot = "/var/lib/containers/storage"

# Run directory
runroot = "/run/containers/storage"
